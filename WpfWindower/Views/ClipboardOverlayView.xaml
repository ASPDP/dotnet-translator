<UserControl x:Class="WpfWindower.Views.ClipboardOverlayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="200"
             d:DesignWidth="300">
    <Border x:Name="OverlayBorderElement" Background="#80000000" CornerRadius="10" Padding="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock x:Name="MessageTextBlock"
                       Grid.Row="0"
                       Text=""
                       Foreground="White"
                       FontSize="20"
                       TextWrapping="Wrap"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Center"
                       TextAlignment="Center" />

            <Button x:Name="CloseButtonElement"
                    Grid.Row="0"
                    Content="x"
                    Background="#66000000"
                    Foreground="White"
                    BorderBrush="Transparent"
                    FontWeight="Bold"
                    Padding="0"
                    Click="CloseButtonElement_Click"
                    Margin="0,-4,-3,0"
                    HorizontalAlignment="Right"
                    Width="17"
                    Height="16"
                    VerticalAlignment="Top" />
        </Grid>
    </Border>
</UserControl>
